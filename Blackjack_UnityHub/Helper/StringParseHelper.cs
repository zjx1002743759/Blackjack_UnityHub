namespace Blackjack_UnityHub.Helper
{
    using HZH_Controls;
    using System.Collections.Generic;
    using System.Linq;
    using System.Text.RegularExpressions;

    public class StringParseHelper
    {
        /// <summary>
        /// 根据正则查找字符串.
        /// </summary>
        /// <param name="strtContent">字符串内容.</param>
        /// <param name="strRegex">正则表达式.</param>
        /// <returns>匹配到的数据.</returns>
        //public static List<Match> RegexParseString(string strtContent, string strRegex)
        //{
        //    Regex rx = new Regex(strRegex);
        //    return rx.Matches(strtContent).ToList();
        //}
    }
}
